# ExtensionMain — 扩展入口基类

- **文件位置**: `kernel/Foundation/Extension/ExtensionMain.php`
- **命名空间**: `kernel\Foundation\Extension`
- **是否可继承**: 是

扩展入口的标记基类。当前为空类，无属性与方法。扩展开发者可将扩展的主入口类继承自本类，作为统一标识，以便框架通过 `instanceof` 识别扩展入口。

## 属性

- 无。

## 方法

- 无。
